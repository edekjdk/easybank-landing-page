import { ImageSection } from './ImageSection';
import { StyledHeader } from './style/Header.styled';

export const Header = () => {
  return (
    <StyledHeader>
      <ImageSection />
    </StyledHeader>
  );
};
